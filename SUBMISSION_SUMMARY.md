# CourseMind 项目总结（提交版）

## 一句话定位

> **CourseMind 把课件变成可追问的学习画布，帮学生找到自己的易忘点**

---

## 解决的问题

### 痛点描述

学生复习时面临的核心困境：
1. **重点不清**：面对整本课件，不知道哪些是核心知识点
2. **理解困难**：对某个知识点不理解，传统问答只能问一次，无法深入
3. **缺乏个性化**：不知道自己的薄弱环节在哪里，复习效率低
4. **来源模糊**：AI 回答不知道来自哪里，无法验证和追溯

### 目标用户

- 大学生、研究生复习考试
- 在线学习者整理课程笔记
- 需要反复理解复杂概念的学习者

---

## 核心功能

### 1. AI 学习画布 🎨（核心创新）

**功能描述**：
- 从课件自动提炼 6-10 个核心知识点
- 用彩色卡片可视化组织（定义、公式、例子、易错点、考点）
- 每个知识点显示来源页码和原文片段
- 点击卡片局部追问，回答作为子卡片回写到画布
- 多次追问自动标记"易忘点"

**价值**：
- ✅ 降低认知负担：卡片化比列表更直观
- ✅ 局部深入：针对单个知识点反复追问
- ✅ 个性化：易忘点反映学生自己的薄弱环节
- ✅ 可追溯：每个知识点都有来源证据链

**技术实现**：
- Prompt 工程：让 AI 按指定格式提炼知识点
- Fallback 机制：AI 失败时使用内置演示数据，保证 Demo 不中断
- 前端状态管理：卡片间父子关系、追问计数

### 2. 知识库 RAG 系统 📚（技术底座）

**功能描述**：
- 上传文档（TXT、Markdown、PDF）
- 自动解析、分块、向量化
- 基于 pgvector 的语义检索
- 问答时显示来源引用和相似度

**价值**：
- ✅ 真实 RAG 实现，不是伪造
- ✅ 完整的向量数据库（pgvector）
- ✅ 可扩展到更多文档格式

**技术实现**：
- 文档解析：PyPDF2（PDF）、直接读取（TXT/Markdown）
- 分块策略：递归分块，保留上下文
- Embedding：DashScope text-embedding-v3
- 检索：pgvector 余弦相似度 + 阈值过滤

### 3. 用户系统 🔐

**功能描述**：
- 注册、登录、Token 刷新
- 文档所有权隔离
- JWT 认证

**价值**：
- ✅ 多用户支持
- ✅ 数据隐私保护
- ✅ 完整的 MVP 认证流程

**技术实现**：
- JWT Access Token（15 分钟）+ Refresh Token（7 天）
- HTTPOnly Cookie 防止 XSS
- 数据库外键约束保证隔离

### 4. 多语言支持 🌏

**功能描述**：
- 完整的中英双语 UI
- 一键切换，实时生效
- 包括知识卡片、标签、按钮、提示

**价值**：
- ✅ 适合国际化场景
- ✅ 留学生友好

**技术实现**：
- vue-i18n
- 零硬编码文本
- Fallback 数据根据语言动态生成

---

## 技术架构

### 系统架构图

```
┌─────────────────────────────────────────┐
│            用户浏览器                      │
│  http://127.0.0.1:5174                  │
└──────────────┬──────────────────────────┘
               │ HTTP REST API
               ↓
┌─────────────────────────────────────────┐
│        前端 (Vue 3 + Vite)               │
│  - 学习画布组件                            │
│  - 知识库问答                              │
│  - 文档管理                                │
│  - 多语言 (vue-i18n)                      │
└──────────────┬──────────────────────────┘
               │ RESTful API (JWT)
               ↓
┌─────────────────────────────────────────┐
│        后端 (FastAPI)                    │
│  - 用户认证 (JWT)                         │
│  - 文档解析与分块                          │
│  - RAG 服务                               │
│  - 向量检索                                │
└──────┬──────────────────┬───────────────┘
       │                  │
       │                  │ API 调用
       ↓                  ↓
┌─────────────┐    ┌──────────────────┐
│ PostgreSQL  │    │   DashScope      │
│ + pgvector  │    │  - Qwen LLM      │
│  - 用户表    │    │  - Embedding     │
│  - 文档表    │    └──────────────────┘
│  - 向量表    │
└─────────────┘
```

### 技术栈

**前端**：
- Vue 3 (Composition API)
- TypeScript
- Vite
- Element Plus
- vue-i18n

**后端**：
- Python 3.11
- FastAPI
- SQLAlchemy (ORM)
- Alembic (迁移)
- PyJWT

**数据库**：
- PostgreSQL 17
- pgvector 扩展

**AI**：
- 阿里云 DashScope
- Qwen 大模型（对话生成）
- text-embedding-v3（向量化）

**部署**：
- Docker
- Docker Compose

### 核心技术决策

1. **为什么用 pgvector？**
   - 开源、成熟、性能好
   - 原生 PostgreSQL 扩展，减少技术栈
   - 支持余弦相似度、欧氏距离等多种度量

2. **为什么用 DashScope 而不是 GPT？**
   - 国内可用，响应快
   - 成本更低
   - API 稳定

3. **为什么用 FastAPI？**
   - 原生 async/await，性能好
   - 自动生成 OpenAPI 文档
   - 类型提示友好

4. **为什么用 Vue 3 Composition API？**
   - 更好的 TypeScript 支持
   - 逻辑复用更灵活
   - 代码组织更清晰

---

## AI 使用方式

### Embedding（文档向量化）

**模型**：`text-embedding-v3`

**流程**：
1. 文档上传后解析为纯文本
2. 递归分块（每块 800-1000 字符）
3. 调用 DashScope Embedding API
4. 向量存储到 pgvector（维度 1024）

**关键代码**：
```python
# 调用 Embedding API
response = dashscope.TextEmbedding.call(
    model='text-embedding-v3',
    input=texts
)
embeddings = [item['embedding'] for item in response.output['embeddings']]

# 存储到 pgvector
chunk = DocumentChunk(
    document_id=doc_id,
    content=text,
    embedding=embedding  # pgvector 自动处理
)
db.add(chunk)
```

### LLM（问答生成）

**模型**：`qwen-turbo`

**流程**：
1. 用户提问
2. 向量检索相关文档片段（TOP-5）
3. 构造 Prompt（包含问题 + 文档上下文）
4. 调用 Qwen 生成回答
5. 返回回答 + 来源引用

**Prompt 示例**：
```
请基于以下课件内容回答问题。如果内容不足，请明确说明。

课件内容：
[文档片段 1]
[文档片段 2]
...

问题：什么是向量空间？

要求：
1. 回答要适合学生复习
2. 尽量引用课件中的原文
3. 如果课件内容不足，请说明
```

**关键特性**：
- Context 长度控制（最大 12000 字符）
- 资料不足判断（相似度 < 0.3）
- 超时重试机制

### Fallback 机制

**设计理念**：保证 Hackathon Demo 不中断

**实现**：
1. **演示画布**：前端内置 6 张卡片（线性代数主题）
2. **追问失败**：生成演示子卡片，明确标注"演示模式"
3. **文档处理失败**：状态显示 `failed`，可重新处理

**为什么需要 Fallback**：
- 现场网络可能不稳定
- API Key 可能没配置
- AI 响应可能超时
- 保证评委看到完整闭环

---

## 当前 MVP 边界

### ✅ 已完整实现

**学习画布**：
- 内置演示数据（6 张线性代数卡片）
- 追问功能（真实 AI + fallback）
- 子卡片回写画布
- 易忘点标记（基于追问次数）
- 来源显示（页码 + 原文片段）
- 多语言切换

**知识库系统**：
- 文档上传（TXT、Markdown、PDF）
- 文档解析和分块
- DashScope Embedding
- pgvector 向量检索
- RAG 问答（带来源引用）
- 文档管理（查看、删除、重新处理）

**用户系统**：
- 注册、登录、登出
- JWT 认证
- Token 自动刷新
- 文档权限隔离

**工程质量**：
- Docker Compose 一键启动
- 自动数据库迁移
- 健康检查
- RESTful API
- OpenAPI 文档

### ⚠️ 需要 API Key 的功能

如果没有配置 `DASHSCOPE_API_KEY`：
- ✅ **可用**：登录、演示画布、追问 fallback、多语言
- ❌ **不可用**：文档 embedding、真实 RAG 问答、基于文档生成画布

### ❌ 当前不支持

**文档格式**：
- PPTX、DOCX（需要额外解析库）
- 图片 OCR（需要视觉 API）
- 手写笔记识别

**画布功能**：
- 真正无限画布（当前是网格布局）
- 拖拽排序（当前是点击选中）
- 卡片连线（当前是视觉层级）

**学习者模型**：
- 当前易忘点基于追问次数（≥2 次）
- 未实现：遗忘曲线、知识图谱、长期记忆

**其他**：
- 课外知识联网搜索（仅基于上传文档）
- 多模态（图片、视频）
- 社交功能（分享、协作）

---

## 未来路线图

### 短期优化（1-2 周）

1. **文档格式扩展**
   - 支持 PPTX 解析
   - 支持 DOCX 解析
   - 优化 PDF 表格识别

2. **画布交互增强**
   - 卡片拖拽排序
   - 手动连线
   - 导出为图片

3. **易忘点算法优化**
   - 引入时间衰减
   - 综合答题正确率
   - 知识点关联分析

### 中期扩展（1-3 个月）

1. **完整学习者模型**
   - 遗忘曲线建模
   - 个性化复习推荐
   - 学习进度追踪

2. **多模态支持**
   - 图片 OCR
   - 视频字幕提取
   - 手写笔记识别

3. **协作功能**
   - 分享学习画布
   - 小组协作学习
   - 知识点评论

### 长期愿景（3-6 个月）

1. **知识图谱**
   - 跨文档知识关联
   - 概念依赖关系
   - 可视化知识地图

2. **智能出题**
   - 基于知识点生成练习题
   - 自适应难度调整
   - 自动批改和反馈

3. **移动端**
   - 响应式设计
   - 移动 App
   - 离线模式

---

## 风险和限制说明

### 技术风险

1. **AI 依赖**
   - 风险：DashScope API 不稳定或涨价
   - 缓解：Fallback 机制、支持多模型切换

2. **向量维度**
   - 风险：text-embedding-v3 维度 1024，数据量大时检索慢
   - 缓解：pgvector 索引优化、分片策略

3. **文档解析准确率**
   - 风险：复杂 PDF 布局解析错误
   - 缓解：支持手动编辑、多格式支持

### 产品风险

1. **易忘点准确性**
   - 风险：当前基于追问次数，可能不准
   - 缓解：明确告知用户算法逻辑、未来引入更复杂模型

2. **隐私安全**
   - 风险：文档包含敏感信息
   - 缓解：本地部署、文档加密、权限隔离

3. **用户习惯**
   - 风险：学生可能不习惯卡片化学习
   - 缓解：提供传统列表视图、用户教育

### 商业风险

1. **市场竞争**
   - 风险：大厂推出类似产品
   - 差异化：聚焦学习场景、画布化组织、易忘点标记

2. **变现模式**
   - 风险：学生付费意愿低
   - 方案：Freemium 模式、机构 B2B、API 服务

---

## 团队和分工

（根据实际情况填写）

- **产品设计**：[姓名]
- **前端开发**：[姓名]
- **后端开发**：[姓名]
- **AI 工程**：[姓名]
- **测试和文档**：[姓名]

---

## 致谢

感谢以下开源项目和服务：

- 阿里云 DashScope
- pgvector
- FastAPI
- Vue 3
- Element Plus
- PostgreSQL

---

## 联系方式

- **项目仓库**：[GitHub 链接]
- **Demo 视频**：[视频链接]
- **团队邮箱**：[邮箱]
- **技术博客**：[博客链接]

---

**最后更新**：2024 年
**Hackathon**：[活动名称]
**团队**：[团队名称]
